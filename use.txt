"use client";

import React, { useEffect, useRef } from 'react';
import { gsap } from 'gsap';
import { ScrollTrigger } from 'gsap/dist/ScrollTrigger';
import { TrendingUp, Sparkles, Languages, BarChart3, PieChart, ArrowRight } from 'lucide-react';
import SectionIntro from '../common/SectionIntro';

gsap.registerPlugin(ScrollTrigger);

const Mission = () => {
  const containerRef = useRef(null);
  const visualRef = useRef(null);
  const textRef = useRef(null);

  useEffect(() => {
    const ctx = gsap.context(() => {
      // Smooth reveal for the whole section
      gsap.from(visualRef.current, {
        x: -60,
        opacity: 0,
        duration: 1.2,
        ease: "power4.out",
        scrollTrigger: {
          trigger: containerRef.current,
          start: "top 80%",
        }
      });

      gsap.from(textRef.current?.children, {
        y: 30,
        opacity: 0,
        stagger: 0.2,
        duration: 1,
        ease: "power3.out",
        scrollTrigger: {
          trigger: containerRef.current,
          start: "top 70%",
        }
      });

      // Continuous floating for glass cards
      gsap.to(".glass-card", {
        y: -15,
        duration: 2.5,
        repeat: -1,
        yoyo: true,
        ease: "sine.inOut",
        stagger: {
          each: 0.5,
          from: "random"
        }
      });
    }, containerRef);

    return () => ctx.revert();
  }, []);

  return (
    <section ref={containerRef} className="relative py-24 px-6 md:px-20 bg-white overflow-hidden">
      {/* Subtle Background Gradient Blur */}
      <div className="absolute top-1/4 -left-20 w-96 h-96 bg-indigo-50 rounded-full blur-[120px] -z-10" />
      <div className="absolute bottom-1/4 -right-20 w-96 h-96 bg-purple-50 rounded-full blur-[120px] -z-10" />

      <SectionIntro badgeText="The Vision" prefix="Our" highlight="Mission" suffix="" />

      <div className="max-w-7xl mx-auto mt-16 grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
        
        {/* LEFT: THE BENTO DASHBOARD VISUAL */}
        <div ref={visualRef} className="lg:col-span-7 relative group">
          <div className="relative aspect-[4/3] w-full bg-zinc-50 rounded-[2.5rem] border border-zinc-100 overflow-hidden shadow-inner">
            
            {/* Main Background Image - "The Interface" */}
            <img 
              src="https://images.unsplash.com/photo-1551288049-bebda4e38f71?auto=format&fit=crop&q=80&w=1200" 
              className="w-full h-full object-cover opacity-40 mix-blend-luminosity grayscale hover:grayscale-0 transition-all duration-1000"
              alt="Analytics" 
            />

            {/* Floating Glassmorphic Cards */}
            <div className="absolute inset-0 p-8 flex flex-col justify-between">
              
              <div className="flex justify-between items-start">
                <div className="glass-card bg-white/70 backdrop-blur-md p-4 rounded-2xl shadow-xl border border-white/50 w-48">
                  <div className="flex items-center gap-3 mb-2">
                    <div className="p-2 bg-indigo-100 rounded-lg text-indigo-600"><BarChart3 size={20} /></div>
                    <span className="text-[10px] font-bold uppercase tracking-widest text-zinc-400">Growth</span>
                  </div>
                  <div className="h-1 bg-zinc-100 rounded-full overflow-hidden">
                    <div className="w-3/4 h-full bg-[#9186BB]" />
                  </div>
                  <p className="mt-2 font-bold text-zinc-800">+124% Engagement</p>
                </div>

                <div className="glass-card bg-zinc-900/90 backdrop-blur-md p-5 rounded-2xl shadow-2xl text-white w-40">
                  <Sparkles size={24} className="text-yellow-400 mb-3" />
                  <p className="text-xs leading-relaxed opacity-80">AI identifies high-value keywords automatically.</p>
                </div>
              </div>

              <div className="flex justify-center">
                <div className="glass-card bg-white/80 backdrop-blur-lg px-6 py-4 rounded-full shadow-2xl border border-white flex items-center gap-4">
                    <div className="flex -space-x-2">
                        {[1,2,3].map(i => (
                            <div key={i} className="w-8 h-8 rounded-full border-2 border-white bg-zinc-200" />
                        ))}
                    </div>
                    <span className="text-sm font-semibold text-zinc-700">Trusted by 2k+ Creators</span>
                </div>
              </div>

              <div className="flex justify-end">
                <div className="glass-card bg-white/70 backdrop-blur-md p-4 rounded-2xl shadow-xl border border-white/50">
                    <PieChart size={32} className="text-[#9186BB]" />
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* RIGHT: CONTENT SECTION */}
        <div ref={textRef} className="lg:col-span-5 space-y-8">
          <h2 className="text-4xl md:text-5xl font-bold text-zinc-900 leading-[1.1] tracking-tight">
            We bridge the gap between <span className="text-[#9186BB]">data</span> and <span className="italic">creativity.</span>
          </h2>
          
          <p className="text-lg text-zinc-500 leading-relaxed">
            Tags AI doesn’t just show numbers. We provide the <span className="text-zinc-900 font-medium">context</span> behind the clicks, helping you understand the human intent driving your growth across every platform.
          </p>

          <div className="grid grid-cols-2 gap-6 pb-4">
            <div>
                <h4 className="font-bold text-2xl text-zinc-900">98%</h4>
                <p className="text-xs text-zinc-400 uppercase tracking-widest mt-1">Accuracy Rate</p>
            </div>
            <div>
                <h4 className="font-bold text-2xl text-zinc-900">24/7</h4>
                <p className="text-xs text-zinc-400 uppercase tracking-widest mt-1">Real-time Analysis</p>
            </div>
          </div>

          <button
            className="group relative inline-flex items-center gap-3 bg-zinc-900 text-white px-8 py-4 rounded-full font-bold transition-all hover:bg-[#9186BB] active:scale-95"
          >
            Explore the Platform
            <ArrowRight size={20} className="group-hover:translate-x-1 transition-transform" />
          </button>
        </div>

      </div>
    </section>
  );
};

export default Mission;



  {/* Feature Grid */}
          <div ref={featuresRef} className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            {features.map(({ icon: Icon, title, desc, id }) => (
              <div
                key={id}
                id={`fc-${id}`}
                className="feature-card group flex flex-col gap-3 p-4 rounded-2xl border border-zinc-200 bg-white/70 backdrop-blur-sm cursor-default"
                style={{ transition: 'border-color 0.3s' }}
                onMouseEnter={() => onCardEnter(id)}
                onMouseLeave={() => onCardLeave(id)}
              >
                <div
                  id={`fi-${id}`}
                  className="w-10 h-10 rounded-xl bg-zinc-50 border border-zinc-200 flex items-center justify-center text-zinc-900"
                >
                  <Icon size={18} />
                </div>
                <div>
                  <h4 className="font-bold text-zinc-900 text-sm mb-1">{title}</h4>
                  <p className="text-xs text-zinc-500 leading-relaxed">{desc}</p>
                </div>
              </div>
            ))}
          </div>

          {/* CTA row */}
          <div className="flex items-center gap-4 pt-2">
            <button className="group flex items-center gap-2 bg-red-600 hover:bg-red-700 text-white text-sm font-bold px-5 py-3 rounded-full transition-all duration-300 shadow-lg shadow-red-200 hover:shadow-red-300 hover:scale-[1.03]">
              See How It Works
              <svg viewBox="0 0 1024 1024" className="w-4 h-4 fill-white group-hover:translate-x-0.5 transition-transform">
                <path d="M800 512H160a32 32 0 0 1 0-64h640a32 32 0 1 1 0 64z"/>
                <path d="m786.752 512-265.408-265.344a32 32 0 0 1 45.312-45.312l288 288a32 32 0 0 1 0 45.312l-288 288a32 32 0 1 1-45.312-45.312L786.752 512z"/>
              </svg>
            </button>
            <span className="text-xs text-zinc-400 font-medium">No credit card required</span>
          </div>